# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.playwright-mcp/

# next.js
.next/
out/
.vercel
next-env.d.ts
game-assets/sprites-wip/
funnel-combo.png

# local Claude Code settings (per-machine)
.claude/settings.local.json

# Vite dep cache (unrelated to this Next.js project — was accidentally
# committed) and one-off review screenshots; neither belongs in the repo
.vite/
_model-review/
# stray review/audit screenshots dropped at the repo root (real assets live in
# public/) — keep them out of the tree and out of `git add .`
/*.png
/*.jpg
/*.jpeg
/*.webp

# secrets — never commit
.env
.env.*
!.env.example
